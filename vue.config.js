const { defineConfig } = require('@vue/cli-service');

module.exports = defineConfig({
  transpileDependencies: [
    // Ajoutez ici les dépendances à transpirer, par exemple :
    'dependency-name'
  ],
  // Ajoutez d'autres configurations ici
});
